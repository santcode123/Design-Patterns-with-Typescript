export enum PlayerType {
    BATTTER = 1,
    BOWLER,
    ALLROUNDER
}

export enum RunType {
    NONE = 0,
    ONE = 1,
    TWO = 2,
    THREE = 3,
    FOUR = 4,
    SIX = 6
}

export enum BallType {
    NORMAL = 1,
    NO_BALL,
    WIDE,
}

export enum WicketType {
    BOWLED = 1,
    CATCH_OUT,
    LBW,
    RUN_OUT
}