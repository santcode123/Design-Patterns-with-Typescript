export enum SpotStatus {
    AVAILABLE = 1,
    OCCUPIED = 2,
    UNDER_MAINTANCE = 3
}

export enum SpotType {
    TWO_WHEELER = 2,
    FOUR_WHEELER = 4
}

export enum VehicleType {
    FOUR_WHEELER = 4,
    TWO_WHEELER = 2
}

export enum PaymentStatus {
    PAID = 1,
    UNPAID = 2
}